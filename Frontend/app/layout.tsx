import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Clinote AI — Safe AI Summaries",
  description:
    "Clinote AI reads clinical documents and produces safe discharge summary drafts. No guessing. No assumptions. Always a draft for human review.",
  keywords: ["clinical AI", "discharge summary", "healthcare AI", "safe AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
